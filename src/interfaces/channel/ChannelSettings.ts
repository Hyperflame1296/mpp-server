interface ChannelSettings {
    allowBots: boolean
    chat: boolean
    color: string
    color2: string
    crownsolo: boolean
    limit: number
    lobby: boolean
    'no cussing': boolean
    noindex: boolean
    visible: boolean
}
export {
    ChannelSettings
}