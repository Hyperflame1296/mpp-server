// import: local interfaces
import { ChannelSettings } from './ChannelSettings.js'

// declaration
interface Channel {
    count: number
    id: string
    _id: string
    settings: ChannelSettings
}
export {
    Channel
}